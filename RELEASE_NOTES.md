## 书径 v0.1.2 (Android)

简约毛玻璃风格的本地阅读器，兼容阅读 3.0 书源。

**本版更新**
- 新增：净化规则（参考阅读3.0替换规则）——正则清除正文广告、水印、"本章完"等杂质，支持预置规则一键导入、JSON 导入导出
- 新增：书源批量管理——长按/多选批量启用、禁用、删除、导出
- 优化：净化在显示时应用，不污染缓存，改规则立即生效

**安装说明**：下载下面的 `shujing-v0.1.2.apk`，在手机上点击安装（覆盖安装不丢数据）。
首次安装需在系统设置中允许「未知来源应用」安装。

- 架构：**arm64-v8a 单架构**（2016 年后的绝大多数 Android 手机都是 arm64）
- 构建环境：Flutter 3.47.3 (stable)
- 产物来自 GitHub Actions 自动构建，并同步发布到 Gitee
