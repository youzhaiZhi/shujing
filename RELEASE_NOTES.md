## 书径 v0.1.1 (Android)

简约毛玻璃风格的本地阅读器，兼容阅读 3.0 书源。

**本版更新**
- 修复：导入书源后搜索不到书籍（规则引擎重写，完整支持 `@` 链式选择器 / `&&` / xpath / JSON / 正则替换）
- 修复：GBK 编码小说站正文乱码
- 修复：搜索失败时显示具体原因（网络 / 超时 / 无结果）
- 视觉：毛玻璃效果仅保留底部导航栏，其余页面改为简洁卡片

**安装说明**：下载下面的 `shujing-v0.1.1.apk`，在手机上点击安装（覆盖安装不丢书架数据）。
首次安装需在系统设置中允许「未知来源应用」安装。

- 架构：**arm64-v8a 单架构**（2016 年后的绝大多数 Android 手机都是 arm64）
- 构建环境：Flutter 3.47.3 (stable)
- 产物来自 GitHub Actions 自动构建，并同步发布到 Gitee
